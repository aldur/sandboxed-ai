use flake

# nix-direnv only invalidates on flake.nix/flake.lock, but the devShell's
# `sandboxed-ai` wrapper bakes sandbox.sh and the seatbelt profiles into the
# store — without watching them, edits keep serving a stale bundle from PATH.
watch_file sandbox.sh
watch_file profiles/*.sb
